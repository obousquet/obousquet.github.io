# Other live buckets around finite signatures

This file was split out of the former monolithic `m6d2-finite-signatures.md`. Load it only when the task specifically touches this finite-signature route.

Other live buckets remain conditional/refined topology of derived non-pure complexes (`sh(H)`,
Scarf/irredundant set-teaching, explicitly defined teaching complexes) and cubical properties of
`X_H`, `X_{H^c}`, and `Q(H)` after fixing conventions and hypotheses.
