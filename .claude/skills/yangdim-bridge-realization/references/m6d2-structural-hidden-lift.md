# Structural route, signed conflict, and hidden-lift counts

This file was split out of the former monolithic `m6d2-finite-signatures.md`. Load it only when the task specifically touches this finite-signature route.

The next work is split against the structural route, which needs provenance type
classification, absence of bad outside-pair overlaps, connected coherent
overlap covering all triples, and a cut-nontriviality proof on the connected
cubic dual active-row graphs.  Its optional
`--signature-system` diagnostic verifies the stronger finite reduction: for
every one of the `5952` ordered disjoint pairs `(K,K^vee)` of antipodal
`10`-cycle bridges, the frontier equality constraints have rank `9` on the
ten triple variables, hence exactly two equality solutions; the dual-bridge
nonconstant constraints reject none of them.  This gives `11904` compatible
signatures total.  `lem:antipodal_ten_cycle_prefix` proves the bridge normal form:
every antipodal isometric `10`-cycle in `Q_5` is generated by a starting
vertex and a coordinate ordering, giving the `192` bridge count
`2^5*5!/(2*10)`.  The rank-nine part is now promoted as
`prop:first_middle_rank_nine_certificate`: after normalizing one bridge, the
`31` disjoint second bridges split into four stabilizer types, a displayed
spanning tree in each type proves the frontier equality graph is connected,
and a four-row witness table checks that the two equality solutions satisfy
every dual-bridge nonconstant constraint.  Thus the full finite sign system
has exactly two accepted sign vectors for every ordered disjoint bridge pair.
The bridge-pair/sign classification is promoted as
`cor:first_middle_bridge_pair_parameterization`: labelled compatible
`(m,d)=(5,2)` sides are in bijection with ordered disjoint antipodal
`10`-cycle bridge pairs and one global sign choice, giving
`192*31*2=11904` labelled sides.  The higher-rank
target is still to find an intrinsic explanation/parameterization of
compatible frontier orientations relative to `(K,K^vee)`.  The first local
rule for this transversal is `cor:antipodal_frontier_orientation`: on every
`(d+1)`-coordinate projection, `K` is an antipodal co-pair and `P` fills
exactly one of its two holes; dually, on every `(m-d)`-coordinate projection,
`K^vee` is an antipodal co-pair and `bar(P)` fills exactly one of its two
holes.  The cross-slice constraint has a maximum normal form
(`cor:antipodal_cross_slice_maximum_normal_form`): for each `y,c`,
`C_{y,c}=0*A_{y=c} union 1*bar(A_{y=1-c})` is maximum VC `d`; its twisted
projection `A_{y=c} union bar(A_{y=1-c})` is maximum VC `min(d,|Y|-1)`, and
its vertical bridge `A_{y=c} cap bar(A_{y=1-c})` is empty for `d=0` and
otherwise maximum VC `d-1`.  The `x`-edges and cubical cylinder of each
cross-slice are indexed by this twisted bridge.  The sharp local obstruction
form is `cor:antipodal_cross_slice_top_obstruction`: because every
cross-slice has Sauer-tight size `sum_{i<=d} binom(|Y|,i)`, compatibility is
equivalent to excluding all top shattering in the cross-slice; equivalently,
for every `y,c`, the twisted projection `A_{y=c} union bar(A_{y=1-c})`
shatters no `(d+1)`-set, and the two twisted fibres `A_{y=c}` and
`bar(A_{y=1-c})` shatter no common `d`-set.  The open antipodal-suspension
classification starts in the middle side VC dimensions
`2 <= VC(A) <= |Y|-3`.
A signed-conflict saturation shortcut is now available for the normalized
`(6,2)` rank route: for any proposed row-potential support `S`, an
independent set in the corresponding signed-conflict graph on the `26`
prefix rows has size at most `10`.  The fixed potentials define consistent
missing triple signs on covered triples; orienting all rows in the independent
set together with `K` gives a class on six coordinates shattering no triple,
so Sauer gives `12+|I|<=22`.  Therefore the finite signed-conflict verifier
is needed for the Sauer-tight size-ten enumeration and orbit match, not for
the upper bound `alpha<=10`.  The companion verifier
`--verify-m6d2-signed-conflict-first-middle-recognition` checks the current
sharp finite recognition statement: every size-ten independent set in the
four canonical conflict graphs has complementary dual row set whose
projection along each hidden axis is one of the `11904` classified
first-middle `(5,2)` compatible sides.  This recognition direction is now
conceptually explained by the tight-signature lemma plus twisted dual
recognition; the remaining prescribed-bridge direction starts with
suspended first-middle `D` disjoint from `K` and must construct `beta` with
outside rows `O=R_beta`.  The strongest
finite normal form is
`--verify-m6d2-global-equality-signature-classification`: it enumerates all
`2^20` triple-hole signatures for the normalized prefix bridge, finds
coherent-row count profile
`0^388608,1^346544,2^168432,3^76720,4^36912,5^15888,6^8876,7^3312,8^2376,10^908`,
and checks that the `908` Sauer-tight signatures produce exactly the `454`
normalized suspended first-middle outside-row sets, in `43` stabilizer
orbits.  The sharpest current finite prescribed-bridge reformulation is
`--verify-m6d2-hidden-lift-count`: for a hidden presentation
`D=Susp_h(B)`, with `T=Q_5\B` and `R=K_{h=0}`, every one of the `258`
orbit-level hidden presentations has exactly two splits
`T=U union V`, `U cap V=R`, for which `0*U union 1*bar(V)` is maximum
VC-two.  The conceptual crux is to prove this two-lift theorem, equivalently
the signed row-overlap cocycle, from suspended first-middle provenance.
